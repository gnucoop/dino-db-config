table:
  name: form_schema_deps
  schema: public
array_relationships:
  - name: form_schemas
    using:
      foreign_key_constraint_on:
        column: form_schema_deps_ref_id
        table:
          name: form_schema
          schema: public
insert_permissions:
  - role: user
    permission:
      check: {}
      columns:
        - _deleted
        - is_deleted
        - created_at
        - _attachments
        - deps_origin
        - metric_data_to_show
        - _rev
        - updated_at
        - id
select_permissions:
  - role: user
    permission:
      columns:
        - _deleted
        - is_deleted
        - created_at
        - _attachments
        - deps_origin
        - metric_data_to_show
        - _rev
        - updated_at
        - id
      filter: {}
update_permissions:
  - role: user
    permission:
      columns:
        - _deleted
        - is_deleted
        - created_at
        - _attachments
        - deps_origin
        - metric_data_to_show
        - _rev
        - updated_at
        - id
      filter: {}
      check: {}
